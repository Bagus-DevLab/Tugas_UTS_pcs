meta {
  name: Delete Disease (Admin)
  type: http
  seq: 8
}

delete {
  url: {{private_url}}/admin/knowledge-base/diseases/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Requires Pakar or Super Admin role
}
